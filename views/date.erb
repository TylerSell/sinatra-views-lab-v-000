<!DOCTYPE html>
<html>
  <body>
    <% month = Date.today.strftime(%B) %>
    <% weekday = Date.today.strftime(%A) %>
    <% number = Date.today.strftime(%d) %>
    <% year = Date.today.strftime(%Y) %>
    <h1>Today</h1>
    <p><% "The date is #{weekday}, #{month} #{number}, #{year}." %></p>
  </body>
</html>